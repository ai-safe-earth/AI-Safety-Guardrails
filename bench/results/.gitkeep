Raw per-repo reports land here: <name>.json and <name>.sarif.

Generated by `python bench/run.py` and gitignored -- see the note in
/.gitignore. bench/score.py reads the .json files to quote false positives
verbatim, so run the harness before scoring.
