# Installation

Install the package from PyPI:

```bash
pip install foo
```

Or with the MCP extras, which pull in the server runtime:

```bash
npx -y foo-mcp-server
```

Both commands are shown unpinned here for brevity; pin them in your own lockfile.
